# creation of vectors of the number of total florets (viable and not) produced by gracilis and californica in the bottom environment
HI.BotGrac.JR <- HI.JR[HI.JR$habitat=="Bottom" & HI.JR$ecotype=="Grac", 9] # 9 corresponds to the row titled fr1 (total # florets produced).
4+3
gr <- read.csv("/Users/juliaharencar/Google Drive (jharenca@ucsc.edu)/Costus/CBB Greenhouse/Growth_rate.csv", header = T)
View(gr)
gr <- gr %>%
filter(!is.na(date)) %>%
write_xlsx("growth_rate.xlsx")
library(dplyr)
gr <- gr %>%
filter(!is.na(date)) %>%
write_xlsx("growth_rate.xlsx")
library("writexl")
gr <- gr %>%
filter(!is.na(date)) %>%
write_xlsx("growth_rate.xlsx")
getwd()
source('~/Google Drive (jharenca@ucsc.edu)/Costus/Seq_analysis/data_manipulation_using_dplyr.R')
source('~/Google Drive (jharenca@ucsc.edu)/Costus/Seq_analysis/NGSadmix_plot.R')
?scan()
## plotting with R
# Get ID and pop info for each individual
pop<-scan("AVH_only.poplabel")
,what="theFuck"
## plotting with R
# Get ID and pop info for each individual
pop<-scan("AVH_only.poplabel",what="theFuck")
## plotting NGSadmix of LGAVH
pop<-scan("LGAVH.poplabel", what="theFuck")
# Read inferred admixture proportions file
q<-read.table("LGAVH_NGSadmix_out.qopt")
# Read inferred admixture proportions file
q<-read.table("LGAVH_NGSadmix_out.qopt")
# Plot them (ordered by population)
ord = order(pop)
par(mar=c(7,4,1,1))
barplot(t(q)[,ord],col=c("red","yellow", "purple", "blue"),names=pop[ord],las=2,ylab="AVH Admixture proportions",cex.names=0.75)
barplot(t(q)[,ord],col=c("red","yellow", "purple", "blue"),names=pop[ord],las=4,ylab="AVH Admixture proportions",cex.names=0.75)
t(q)[,ord]
## plotting NGSadmix of LGAVH
pop<-scan("LGAVH.poplabel", what="theFuck")
# Read inferred admixture proportions file
q<-read.table("LGAVH_NGSadmix_out.qopt")
## plotting with R
# Get ID and pop info for each individual
pop<-scan("AVH_only.poplabel", what="theFuck")
# Read inferred admixture proportions file
q<-read.table("AVH_only_NGSadmix_out.qopt")
## plotting NGSadmix of LGAVH
pop<-scan("LGAVH.poplabel", what="theFuck")
# Read inferred admixture proportions file
q<-read.table("LGAVH_NGSadmix_out.qopt")
## plotting with R
# Get ID and pop info for each individual
pop<-scan("AVH_only.poplabel", what="theFuck")
## plotting NGSadmix of LGAVH
pop<-scan("LGAVH.poplabel", what="theFuck")
# Read inferred admixture proportions file
q<-read.table("LGAVH_NGSadmix_out.qopt")
# Plot them (ordered by population)
ord = order(pop)
par(mar=c(7,4,1,1))
barplot(t(q)[,ord],col=c("red","yellow", "purple", "blue"),names=pop[ord],las=2,ylab="AVH Admixture proportions",cex.names=0.75)
quartz.save("LGAVH_admixture_plot", type="png")
